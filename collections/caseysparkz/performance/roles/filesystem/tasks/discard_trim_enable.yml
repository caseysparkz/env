---
- name: "Enable fstrim.timer"
  become: true
  when: "ansible_facts.system == 'Linux'"
  tags:
    - "filesystem"
    - "fstrim"
    - "systemd"
  ansible.builtin.systemd:
    name: "fstrim.timer"
    state: "started"
    enabled: true
    daemon_reload: true
