---
- name: "Install vim"
  become: true
  tags: ["apt"]
  when: "ansible_facts.os_family == 'Debian'"
  ansible.builtin.apt:
    pkg: "vim"
    state: "present"
    update_cache: true
    cache_valid_time: 3600
